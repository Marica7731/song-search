name: Auto Update Song List

on:
  schedule:
    - cron: '0 * * * *' # 每小时执行
  workflow_dispatch: # 手动触发

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 安装依赖并运行脚本
        run: |
          # 进入scripts目录
          cd scripts
          # 初始化npm（静默模式）+ 安装cheerio
          npm init -y --silent && npm install cheerio --silent
          # 运行更新脚本
          node update-songs.js

      - name: 提交并推送更新
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          directory: data
          message: "🤖 自动更新歌单数据（视频页DOM解析）"
