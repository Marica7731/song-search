name: Auto Update Song List

on:
  schedule:
    # 【修改点】每小时执行一次 (UTC 时间，每小时的第 0 分)
    - cron: '0 * * * *'
  workflow_dispatch: # 保留手动触发功能

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Update Script
        run: node scripts/update-songs.js

      - name: Commit and Push Changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Auto Bot"
          git add data/
          # 只有当文件发生变化时才提交，避免每小时产生一次空提交
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 自动更新歌单数据" && git push)
