name: Auto Update Song List

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      # 1. 彻底清空运行环境，重新克隆仓库（用PAT）
      - name: Clean and Clone Repo
        run: |
          rm -rf *
          git clone https://${{ secrets.PAT_TOKEN }}@github.com/Marica7731/song-search.git .
          git checkout main

      # 2. 配置Node环境
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. 运行脚本生成新文件
      - name: Run Update Script
        run: node scripts/update-songs.js

      # 4. 提交并推送（用PAT，无冲突）
      - name: Commit and Push with PAT
        run: |
          git config --local user.email "your-github-email@xxx.com"  # 替换成你的GitHub绑定邮箱
          git config --local user.name "Marica7731"  # 替换成你的GitHub用户名
          git add data/
          # 只有文件变化才提交
          if ! git diff --quiet --staged; then
            git commit -m "🤖 自动更新歌单数据"
            git push https://${{ secrets.PAT_TOKEN }}@github.com/Marica7731/song-search.git main
          fi
