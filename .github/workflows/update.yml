name: Auto Update Song List

on:
  schedule:
    - cron: '0 * * * *' # 每小时执行，可调整
  workflow_dispatch: # 手动触发

permissions:
  contents: write # 赋予提交代码权限

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 安装系统依赖（Chrome运行必备）
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 libatk-bridge2.0-0 libdrm-dev libxkbcommon-dev libgbm-dev libasound-dev libatspi2.0-0 libxshmfence-dev

      - name: 安装Puppeteer（国内镜像加速）
        run: |
          cd scripts
          npm init -y --force
          npm config set puppeteer_download_host=https://npmmirror.com/mirrors
          npm install puppeteer@21.0.0 --save-exact

      - name: 执行更新脚本
        run: |
          cd scripts
          node update-songs.js
        env:
          PUPPETEER_EXECUTABLE_PATH: ./node_modules/puppeteer/.local-chromium/linux-*/chrome-linux/chrome

      - name: 检查数据更新
        id: check_changes
        run: |
          if git status --porcelain | grep -q 'data/'; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: 提交更新（仅当有变化时）
        if: steps.check_changes.outputs.has_changes == 'true'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 自动更新歌单数据"
          file_pattern: "data/*.js data/index.json"
          branch: main
